

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
 
  
</head>
<body>
  <h1>Lista de carro com estrutura de reptição</h1>
  <div id="conteudo">
  </div>
  <!--  <script src="script.js"></script> -->
  <script>
const carros = ["Uno", "Usca", "Escort", "Gol"];

const conteudo = document.getElementById("conteudo");

var dados3 = "";
i3 = 0;


/*função*/
function criaSecao(titulo,dadosF)
{
  let secao = document.createElement("div");
  secao.innerHTML = "<h2>"+titulo+"</h2>" +dados;
  conteudo.appendChild(secao);

}



let dados = "";
let i =0;
while(i < 4)
{
    dados += "<p>"+carros[i]+"</p>";/*Interação repetitiva para pegar a variável*/
    i++;/*função incrementar para não gerar loop*/
}

/*L*/


let dados2 = "";
let i2 =0;
while(i2 < carros.length)/*Pegando o ultimo indice de array com length*/
{
    dados2 += "<p>"+carros[i2]+"</p>";/*Interação repetitiva para pegar a variável*/
    i2++;/*função incrementar para não gerar loop*/
}
criaSecao("Loop while melhoraado", dados2)
const secao2 = document.createElement("div");
secao2.innerHTML = "<h2>Loop While</h2>" +dados2;
conteudo.appendChild(secao2);

/*While aoinvertido*/
var dados3 = "";
i = 0;
do{
  
    dados3 += "<p>"+carros[i2]+"</p>";/*Interação repetitiva para pegar a variável*/
    i++;/*função incrementar para não gerar loop*/

}while(i < carros.length)
criaSecao("Loop while invertido", dados3)

/*For*/
dados = "";
for(i = 0; i<carros.length; i++){
  dados += "<p>"+carros[i]+"</p>";
  

}
criaSecao("Loop For", dados)

/*LAço fo super poderoso*/
dados = "";
for(let carro of carros){
  if(carro === "Escort")
  break;/*Quando chegar no escorte para tudo*/
  /*continue; Quando chega no continue ele não executa o indice do escorte e ele poula o escorte e vao para os outros*/
  dados += "<p>"+carro+"</p>";

}
criaSecao("Loop For of", dados);

/*Laços de repetição for in com matipulação em matri FOR IN*/

let carro1 = {marca:"Ford", modelo: "Ka", ano:"2015"};
let carro2 = {marca:"Fiat", modelo: "Gol", ano:"2000"};

let listaCarros = [];
listaCarros.push(carro1);
listaCarros.push(carro2);

console.log(listaCarros);

/*Aqui esta a versão versão funcional do código de repetição*/

dados = ""; 

// 1. Mudamos carro2 para listaCarros
for(let carro of listaCarros) 
{
    // 2. Inicializamos como string vazia para evitar o "undefined"
    let propriedades = ""; 
    
    for(let prop in carro)
    {
        // 3. Atenção ao nome da variável (propriedades)
        propriedades += carro[prop] + " | ";
    }
    
    dados += "<p>" + propriedades + "</p>";
}
criaSecao("Loop For in", dados);


dados = "";
carros.forEach((carro) =>{
  
  dados += "<p>"+carro+"</p>";
})
criaSecao("Loop Foreach", dados);




  </script> 
</body>

</html>
